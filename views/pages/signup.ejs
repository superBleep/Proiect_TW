<!DOCTYPE html>
<html  lang="ro">
    <head>
        <meta name="description" content="AllMuzica - Înregistrare utilizatori.">
        <meta name="keywords" content="magazein, AllMuzica, inregistrare, utilizatori">
        <title>AllMuzica - înregistrare </title>
        
        <%- include("../fragments/head") %>

        <style>
            form {
                display: inline-block;
                text-align: left;
            }

            form label {
                padding-right: 0.5em;
            }

            form input:not([type="submit"]), form select {
                float: right;
            }
        </style>

        <script>
            window.addEventListener("DOMContentLoaded", function() {
                let p = document.getElementById("eroareSignup");
                if(p) {
                    setTimeout(function() {
                        p.style.display = "none";
                    }, 4000)
                }
            })
        </script>
    </head>

    <body>
        <%- include("../fragments/header") %>

        <main>
            <h1>Inregistrare utilizator</h1>

            <% if(!locals.raspuns) { %>    
                <%-include("../fragments/form", {action: "/signup"}) %>
            <% } %>
            <% if(locals.raspuns) { %>
                <%- locals.raspuns + "<br/>"%>
            <% } %>

            <% if(locals.err) { %>
                <p id="eroareSignup"><%- locals.err %></p>
            <% } %>
        </main>
            
        <%- include("../fragments/footer") %>
    </body>
</html>